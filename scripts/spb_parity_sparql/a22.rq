PREFIX cwork:<http://www.bbc.co.uk/ontologies/creativework/>
# SPB advanced q22 (faceted search) — official fixed BGP (incl. the required
# `cwork:tag`) + full-text word + pinned category/audience + a dateCreated window.
# Tag-less generator output => faithfully empty (matches our a22, which also
# requires a tag edge).
SELECT DISTINCT ?cw WHERE {
  ?cw a cwork:CreativeWork ; cwork:title ?title ; cwork:description ?desc ;
      cwork:category <{{CATEGORY}}> ; cwork:tag ?tag ; cwork:audience <{{AUDIENCE}}> ;
      cwork:liveCoverage ?lc ; cwork:primaryFormat ?pf ; cwork:dateCreated ?dc .
  FILTER(regex(?title, "\\b{{WORD}}\\b", "i") &&
         str(?dc) >= "{{DATE_FROM}}" && str(?dc) <= "{{DATE_TO}}")
}
