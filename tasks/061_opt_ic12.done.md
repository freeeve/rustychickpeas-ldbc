# ic12 — Optimize IC12 (expert search) — DONE

Kùzu: 925 ms -> 281 (~3.3x) ms. Fold the recursive isSubclassOf class test into the main MATCH pattern instead of a per-row EXISTS (a precompute+IN-list variant was worse). Byte-identical (compare.py PASS).
