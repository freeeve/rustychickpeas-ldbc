# 057 — Optimize IC14 (weighted shortest path) [chickpeas] — DONE

chickpeas IC14: ~14 ms -> ~9 ms, now BEATS Kùzu (13 ms) — flips the only IC loss.
Switched from the unidirectional single-source `dijkstra` to the bidirectional
`weighted_shortest_path` (meets in the middle). Direction::Outgoing, not Both:
knows is stored both directions, so Outgoing-forward + Incoming-backward covers
the graph without Both's redundant double-traversal (Both measured ~13 ms).
Returns the path cost only — the comparable metric, and what Kùzu's WSHORTEST
reports. ic14 cross-check still byte-identical. (The per-rel interaction-map
HashMap lookup is ~half the remaining cost; a precomputed rel weight would shave
more, but bidirectional alone wins.)
