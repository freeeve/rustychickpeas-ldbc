# 112 — Optimize IC12: expert search

Per-query optimization loop. Methodology + commands:
[100_ic_perf_methodology.md](100_ic_perf_methodology.md).

## Loop
- [x] 1. Bench allocations: 9,186 allocs / 494,890 bytes
- [x] 2. CPU profile: deep traversal `NeighborsByType::next` ~45% (comments×replyOf×
  hasTag×hasType) + `get_id`/`pstr` string work
- [x] 3. Optimize: collect qualifying tag **ids** (dedup, no String in the loop),
  resolve names only for the top-20 returned
- [x] 4. Re-bench: 1,414 allocs / 302,680 bytes
- [x] 5. Wall-clock A/B: ~38.3 → ~31.7 ms (~17%), same window
- [x] 6. Value-identity: IC emit byte-identical

## Measurements
| metric                  | baseline | after |
|-------------------------|----------|-------|
| allocs                  | 9,186    | 1,414 |
| bytes                   | 494,890  | 302,680 |
| wall-clock median (ms)  | ~38.3    | ~31.7 (~17%) |
| hot fn (CPU %)          | traversal + String work | traversal |

## Notes

The deep per-friend nesting (comments → replyOf → parent's hasTag → hasType class
check) is ~45% and inherent. The avoidable cost: `tags.insert(name.to_string())`
allocated a `String` per qualifying-tag *occurrence*, and the BTreeSet dedup'd
only *after* the allocation — so a tag seen N times wasted N−1 `to_string()`s.
Collect tag ids in a `HashSet<u32>` (dedup in the loop, no String) and resolve
names only for the 20 friends actually returned (sorted). 6.5× fewer allocations,
~17% faster, value-identical.

**Status: done.**
