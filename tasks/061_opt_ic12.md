# 061 — Optimize IC12 (expert search)

chickpeas 48 ms / Kùzu 925 ms.

Kùzu: `isSubclassOf*0..10` recursive class test inside an EXISTS per (friend,
reply,post,tag), plus count(DISTINCT). Precompute the class+descendant TagClass
set once, then test membership.

Timings are SF1 under load avg ~6.7 (read relatively; re-measure on a quiet machine). Acceptance: faster on the targeted engine AND still byte-identical via kuzu/compare.py.
