# ic10 — Optimize IC10 (friend recommendation) — DONE

Kùzu: 1619 ms -> ~1410 ms. Replaced the two-OPTIONAL-MATCH cross product with COUNT{} subqueries + moved NOT-EXISTS after DISTINCT. Marginal/within-noise: the cross product was not the cost; the 2-hop foaf expansion + per-foaf counting is inherent (chickpeas wins it via CSR/bfs ~4ms). EXISTS-in-aggregate / EXISTS-in-WITH-CASE variants segfaulted Kuzu. Byte-identical (compare.py PASS).
