# 056 — Optimize IC10 (friend recommendation)

chickpeas 4 ms / Kùzu 1619 ms.

Kùzu: my two-OPTIONAL-MATCH formulation builds a post x interest-post cross
product per foaf before the DISTINCT counts. Reformulate to count total and
common posts without the cross product (e.g. per-post CASE in a single scan, or
two independent aggregating subqueries joined on foaf).

Timings are SF1 under load avg ~6.7 (read relatively; re-measure on a quiet machine). Acceptance: faster on the targeted engine AND still byte-identical via kuzu/compare.py.
